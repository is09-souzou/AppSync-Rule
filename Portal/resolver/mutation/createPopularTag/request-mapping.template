{
    "version" : "2017-02-28",
    "operation" : "PutItem",
    "key" : {
        ## If object "id" should come from GraphQL arguments, change to $util.dynamodb.toDynamoDBJson($ctx.args.id)
        "tag": $util.dynamodb.toDynamoDBJson($ctx.args.item.tag)
    },
    "attributeValues" : {
    	"itemCount": $util.dynamodb.toDynamoDBJson(0)
  	}
}