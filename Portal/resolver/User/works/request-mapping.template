{
    "version" : "2017-02-28",
    "operation": "Invoke",
    "payload": {
        "field": "connectionWorks",
        "arguments": {
        	"userId": $context.source.id,
            "limit": $context.arguments.limit,
            "exclusiveStartKey": $context.arguments.exclusiveStartKey
        },
        "identity":$utils.toJson($context.identity)
    }
}
